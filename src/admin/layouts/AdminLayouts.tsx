import { Outlet } from "react-router"


const AdminLayouts = () => {
    return (
        <div>
            <h1>Admin Layout</h1>
            <Outlet />
        </div>
    )
}

export default AdminLayouts
